<% $feedbacks.each do |feedback| %>
  <%= feedback.user.name %><br />
  <%= feedback.user.email %><br />
  <%= feedback.content %>
  <%= link_to "delete", feedback_path(feedback.id), :method => :delete, :confirm => "Are you sure?" %>
<% end %>