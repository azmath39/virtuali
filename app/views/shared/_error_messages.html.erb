<% if target.errors.any? %>
<div>
  <h2>
    <%= pluralize(target.errors.count, "error") %>
  </h2>
  <p>There were problems with the following fields:</p>
  <ul>
    <% target.errors.full_messages.each do |messaage| %>
    <li><%= message %></li> 
    <% end %>
  </ul>
</div>
<% end %>