<%= form_for :sim_transaction, :url => AuthorizeNet::SIM::Transaction::Gateway::TEST do |f| %>
  <%= sim_fields(@sim_transaction) %>
  <%= label_tag 'x_amount', "Total: #{number_to_currency(@amount)}" %>
  <br />
  <%= hidden_field_tag("x_description", "Product1", {:id => "x_description"}) %>
  <%= hidden_field_tag("x_email", "jagadishwer@brilliance-works.com", {:id => "x_email"}) %>
  <%= f.submit 'Purchase'%>
<% end %>