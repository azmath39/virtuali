<%= form_for :sim_transaction, :url => AuthorizeNet::SIM::Transaction::Gateway::TEST, :html=>{:id=>"payment_form"} do |f| %>
  <%= sim_fields(@sim_transaction) %>
  <%#= label_tag 'x_amount', "Total: #{number_to_currency(@amount)}" %>
<%=image_tag("359.gif", :alt => "Virtuali", :style=>'margin:20%;margin-left:37%;')%>
  <br />
  <%= hidden_field_tag("x_description", @product, {:id => "x_description"}) %>
  <%= hidden_field_tag("x_email",@email, {:id => "x_email"}) %>
  <%#= f.submit 'Purchase'%>
<% end %>
  <script>
  $(document).ready(function(){
$('form#payment_form').submit();
  });
</script>